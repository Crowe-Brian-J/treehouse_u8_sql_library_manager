doctype html
html
  head
    title Library Manager
    link(rel="stylesheet", href="/stylesheets/style.css")
  body
    header
      h1 Library Manager
      nav(style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px")
        //- Only show home button if not already on /books
        if !hideHomeButton
          a.button(href="/books") Home
        a.button(href="/books/new", style='margin-left: auto;') Create New Book
    main
      block content
