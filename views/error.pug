extends layout

block content
  h2 Something Went Wrong
  p #{err.message || 'An unexpected error occurred.'}
  if err.status
    p Status: #{err.status}
  a.button(href="/books") Return to Book List
